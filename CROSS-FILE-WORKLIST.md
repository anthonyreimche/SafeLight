# Safelight deep-clean audit & fix log

Scan: 236 findings across 99 files.

## Status

- **Tier A (safe cleanups): DONE** — 42 fixes, 7 shared modules extracted, tsc + tests green.
- **Tier B (behavioral bugs): DONE** — 115 fixes across 62 files, tsc + tests green.
- **Cross-file remainders**: 33 items the by-file fixers correctly deferred (need coordinated multi-file edits) — worklist below.
- **Pixel/render tier**: 19 + several deferred render-path items — report-only, need in-app verification.

## Cross-file remainders (from Tier-B skips)

These are real fixes that span multiple files; the single-file fixers flagged them rather than corrupt the build.

### `src/rendering/webgl/renderer.ts`
- **What:** bindSource restores width/height/linear/applyBaseCurve but not the maxEdge the entry was uploaded with; a cache-hit render uses whatever maxEdge the last setImage set (render-worker.ts:90 renderThumbnailFromSource carries a maxEdge that is ignored on the bindSource path).
- **Remainder:** Restoring maxEdge in bindSource changes this.maxEdge, which drives resize() (output canvas resolution) and prepassDims() (prepass render resolution) -> rendered-pixel/output-size change requiring in-app verification (rule 3). It also has a cross-file remainder I may not edit: src/rendering/render-worker.ts 'bindSource' handler (case bindSource, ~line 363) would need to thread msg.maxEdge (add a maxEdge field to the bindSource WorkerRequest at ~line 67 and to renderer.bindSource's signature), and SourceEntry would need a new maxEdge field recorded at upload time in uploadSource. Left entirely unmade to avoid a render-path change and cross-file signature edits owned by a parallel agent.

### `src/rendering/webgl/renderer.ts`
- **What:** Stage textures beyond MAX_STAGE_TEXTURES (line ~1907 break) and prepass stages beyond MAX_PREPASS_STAGES (line ~2554 break) are silently dropped; overflow samplers stay at default unit 0 and sample uImage as LUT/result data, producing garbage with no diagnostic.
- **Remainder:** The substantive fix (bind ensureDummyStageTex() to a safe unit / point the overflow sampler at the dummy) changes what those samplers read and therefore alters WebGL rendered output in the overflow case -> needs separate in-app verification (rule 3). Adding only the console.warn half would fire a warning while still rendering garbage (an incomplete, non-improving change), so the whole finding is skipped rather than manufacturing a partial edit.

### `src/rendering/webgl/renderer.ts`
- **What:** sourceEpoch is bumped on every bindSource (including the re-bind of the same displayed texture at the end of a bind=false prefetch), so the prepass srcSig ('e'+sourceEpoch) changes and expensive prepasses (denoise) are needlessly recomputed for an unchanged image after every neighbour prefetch.
- **Remainder:** This is a render-path prepass-cache change: re-keying srcSig by currentSourceKey (lines 2112 and 2129) risks a stale prepass result when the same key is re-uploaded with different pixels after an edit (uploadSource -> dropSource + setImage currently relies on the epoch bump to invalidate; dropSource does not clear prepassSigs). Getting it wrong yields stale denoise output, so it needs the separate correctness+visual verification rule 3 calls for. The partial 'skip the epoch bump when re-binding the already-active texture' variant also interacts with the prefetch restore path and carries the same stale-cache risk, so no partial edit was made.

### `src/rendering/webgl/renderer.ts`
- **What:** The 8-bit bitmap upload path (line ~1344) ignores maxEdge and uploads the ImageBitmap at full size (unlike the float path's capFloatToEdge and the opt-in srgb16 cap); an oversized bitmap can exceed MAX_TEXTURE_SIZE so texImage2D fails and the frame renders black.
- **Remainder:** The fix draws the bitmap into a downscaled OffscreenCanvas (or clamps to maxTextureEdge) before upload, changing the uploaded texture resolution and thus the rendered pixels (and the black-frame recovery case) -> render-path output change requiring in-app verification (rule 3).

### `electron/main.cjs`
- **What:** F3 (line 578): install dir keyed solely by attacker-controlled manifest.id, and install begins with rmSync of that dir, so any repo can declare an already-installed (even verified) extension's id and replace its code; banned-list is the only gate.
- **Remainder:** Real observation, but the suggested fix is a behavioral/policy design change rather than a minimal bug fix: recording a source owner/repo sidecar and refusing cross-repo id overwrite (unless explicitly confirmed) needs a product decision on the confirmation UX and would silently break legitimate re-installs/updates when a repo is renamed, moved, or installed from a fork. It would also ripple into the renderer install/confirm flow (out of my edit scope). The path-traversal/prefix-escape portion of the risk is already closed by F1 (backslash + normalized-.. rejection and the contains() write guard). No self-contained safe portion to apply in main.cjs alone. Cross-file remainder if pursued: a confirmation UX in the renderer extension-install flow (src/extensions/*, e.g. ExtensionManagerPanel.tsx / loader.ts) plus a main-process sidecar write in installPlugin recording {owner, repo} and an overwrite guard comparing incoming spec repo to the recorded one.

### `src/project/folder-ops.ts`
- **What:** Finding 3: surface a user-visible error (not just a silent no-op) when moveFolder/renameFolder hit a destination collision.
- **Remainder:** The destructive core (silent merge/overwrite) is fixed in folder-ops.ts by bailing before moveOnDisk. Making the collision user-visible the way renamePhoto does would require changing renameFolder/moveFolder from Promise<void> to a result type and updating their only callers, commitRename and handleDrop, in src/modules/library/LibrarySidebar.tsx (lines ~428 and ~448, both currently fire-and-forget `void` calls) to show an alert. That file is outside my allowed edit list. Injecting window.alert directly into folder-ops.ts was rejected because this module deliberately keeps all user-facing alerts in the UI layer (e.g. deleteFolder does not confirm; its caller handleDelete does). Cross-file change needed: in src/modules/library/LibrarySidebar.tsx, consume a new result from renameFolder/moveFolder and window.alert the collision reason.

### `src/project/folder-ops.ts`
- **What:** Finding 5: additionally surface to the user that virtual copies were skipped during Export data (rather than only stopping the clobbering write from being counted).
- **Remainder:** The clobber and the inflated `written` count are fixed by skipping copies inside exportPhotoData (a copy no longer overwrites its master's sidecar, and it is no longer counted as a successful write). Also reporting 'N copies were skipped' would require changing exportPhotoData's return type from number to a shape carrying a skipped count, which forces a change to its only caller, handleExportData in src/modules/library/LibraryGrid.tsx (lines ~169-175, which builds the completion alert from the returned number). That file is outside my allowed edit list. Cross-file change needed: in src/modules/library/LibraryGrid.tsx handleExportData, read the new skipped count and mention skipped copies in the alert.

### `src/ui/components/PreferencesDialog.tsx`
- **What:** Finding 2 (line 1294): Raising 'Cached preview resolution' never re-caches already-present photos because the cache key has no size component and 'Cache all now' (force) skips any key already present regardless of stored size.
- **Remainder:** Confirmed real, but no correct fix is self-contained in PreferencesDialog.tsx (the only file I may edit). The dialog's 'Cache all now' button just calls preDecodeRawsForCache({force:true}); the actual bug is in that function's skip filter. Cross-file remainder (a parallel agent owns these files): In src/modules/library/import-photos.ts, preDecodeRawsForCache (line 714-731) builds `todo` by filtering out photos whose rawCacheKey(p.relPath, p.fileSize, p.rotation) is in `present` (from cachedKeys()). Since rawCacheKey (src/raw/raw-cache.ts:36, `v3:${relPath}:${fileSize}:${rotation}` — no size) ignores stored resolution, a forced re-cache after raising rawCacheMaxEdge skips every already-present photo. Fix: when opts.force is set, do NOT blind-skip present keys; instead read each cached entry's dimensions (add a size-returning variant of cachedKeys / read via readCachedPreview) and re-decode when Math.max(w,h) < Math.min(getSettings().rawCacheMaxEdge, native long edge). Alternatively invalidate/rewrite cache entries smaller than the new cap when rawCacheMaxEdge increases. Note writeCachedPreview (raw-cache.ts:73) already caps writes at rawCacheMaxEdge, so a re-decode will produce a larger blob correctly once the skip is fixed.

### `src/rendering/render-bridge.ts`
- **What:** Finding 1 remainder: computeUpright's promise can still be lost in two cross-file cases owned by render-bridge.ts.
- **Remainder:** render-bridge.ts is NOT in my allowed-edit list (a parallel agent owns it). Two remaining fixes belong there: (1) In handleMessage, the 'error' case (line ~155-157) only calls onError and never settles uprightResolve, so if detectLines/computeUprightCorrection throws in the worker (routed to the generic 'error' response), computeUpright still hangs — the 'error' case should also resolve+clear uprightResolve, OR the worker needs a dedicated uprightError response type (adding that response type to render-worker.ts is possible, but handling it requires editing render-bridge.ts). (2) render-bridge keeps a single `uprightResolve` slot (line 53); a second concurrent computeUpright clobbers the first, permanently hanging it — convert `uprightResolve` to a Map<number, resolver> keyed by a reqId (mirroring captureResolvers/sourceBoundResolvers), thread reqId through the 'analyzeUpright' WorkerRequest and the 'upright' WorkerResponse, and resolve by reqId. The WorkerRequest/WorkerResponse type edits are in render-worker.ts (editable), but the resolver-map logic and computeUpright signature live in render-bridge.ts (off-limits), so the whole change is deferred to keep the two halves consistent.

### `src/rendering/render-worker.ts`
- **What:** Finding 4: 'High bit-depth previews' preference has no effect on the Develop renderer (worker hardcodes highBitDepth: true at the init case; the init message carries only width/height).
- **Remainder:** SKIPPED per the render-pixel rule: the effective fix changes the develop renderer to use 8-bit vs 16-bit (norm16) GPU textures based on the setting, which alters rendered-pixel precision (gradient smoothness) in the WebGL render path and needs separate in-app verification. It is also unavoidably cross-file: the worker cannot read getSettings()/localStorage, so the value must be threaded from the main thread. Required full fix — (a) render-worker.ts (editable): add `highBitDepth?: boolean` to the `init` WorkerRequest variant and pass `msg.highBitDepth ?? true` to `new WebGLRenderer(canvas, { highBitDepth: ... })` in the init case; (b) render-bridge.ts (OFF-LIMITS): change `init(width, height)` to send `highBitDepth: getSettings().highBitDepth` in the `{ cmd: 'init' }` message (getSettings is already imported there). Doing only the worker half (a) is inert today — the field would always be undefined, so pixels stay byte-identical — and leaves a dangling unused optional param with no behavior change, so I left the file untouched rather than half-wire a feature I can't complete or verify. Note the accompanying PreferencesDialog.tsx hint ('Applies when Develop is reopened.') stays inaccurate until (a)+(b) land.

### `src/extensions/loader.ts`
- **What:** Uninstall/install/update not propagated to other windows: a plugin uninstalled in one window keeps running in a detached window until relaunch (contributions live, settings key already deleted).
- **Remainder:** Not a minimal fix. The suggested remedy is a new cross-window broadcast feature: write a versioned install nonce (e.g. sl_ext_installed) to localStorage in installFromGitHub/uninstallPlugin/updateExtension, add a storage listener that re-lists native.plugins and reconciles loaded/registered extensions. A load-bearing half of the correct fix lives OUTSIDE my edit list: src/extensions/ext-settings.ts's initExtSettings storage listener early-returns on `e.newValue == null` (line 85: `if (!e.key?.startsWith(PREFIX) || e.newValue == null) return;`), so it ignores the localStorage removal that deleteExtensionSettings triggers and leaves stale values in the other window's useExtSettings. CROSS-FILE REMAINDER (owned by parallel agent, do not edit): in src/extensions/ext-settings.ts, initExtSettings must handle the removal event — when e.key starts with PREFIX and e.newValue == null, drop that id from useExtSettings (mirror deleteExtensionSettings' setState replace) instead of returning early. Building only the loader.ts nonce+reconciler half in isolation would be a large speculative addition that could conflict with how the ext-settings.ts owner resolves the removal-event gap, so I left it whole to that owner rather than shipping a partial/mismatched protocol.

### `src/modules/library/import-photos.ts`
- **What:** Finding 3 (line ~730): raising the 'Cached preview resolution' (rawCacheMaxEdge) preference never upgrades existing cache entries — preDecodeRawsForCache skips any photo whose key is already present, and Develop's fast path accepts any cached size, so the load-image.ts:243-247 upgrade branch is unreachable during browsing/prefetch.
- **Remainder:** Diagnosis confirmed real, but every viable fix is cross-file and outside my allowed edit list, so no in-file change was made. rawCacheKey (raw-cache.ts) has no resolution component, and the cache worker's keys command (workerCachedKeys, cache-bridge.ts) returns only string[] with no per-entry dimensions. To include under-sized cached photos in the forced prefetch requires ONE of: (A) a new dimensions-exposing cache API spanning src/raw/cache-worker.ts (add a 'sizes'/'entries' response returning stored width/height per key WITHOUT decompressing the gzip blob), src/raw/cache-bridge.ts (add workerCachedEntries), and src/raw/raw-cache.ts (export cachedEntries(): Promise<Map<string,{width:number;height:number}>>), then in preDecodeRawsForCache include photos where the cached long edge < min(getSettings().rawCacheMaxEdge, native long edge) — decodeOne already writes via writeCachedPreview on the same key so the larger buffer overwrites the smaller; OR (B) at Develop's loadPhotoImage(photo, opts) call site (NOT in import-photos.ts) pass opts.minEdge derived from getSettings().rawCacheMaxEdge so the existing load-image.ts:243-247 upgrade path fires on the next Develop open. The only self-contained in-file option — calling readCachedPreview(key) per already-present candidate to read its dimensions — decompresses every gzip blob on each project open, directly violating the documented 'one directory listing, decode nothing' performance contract of preDecodeRawsForCache, so it is a regression rather than a minimal fix and was not applied.

### `src/modules/develop/MaskOverlay.tsx`
- **What:** Colour-range eyedropper (line ~344) passes frame-local LAYOUT px to sampleLinearRGB, which indexes canvas BUFFER pixels, so the mask colour pick samples the wrong pixel whenever displayed size differs from buffer size (any fit/CSS-zoom view, and off by dpr in ROI zoom).
- **Remainder:** The correct fix is inherently cross-file and those files are not in my edit list. Mapping layout px -> buffer px correctly across all three render modes needs ViewportImage's frameToBuffer (or the `visible` rect it already computes) exposed through the overlay render-prop. Required cross-file changes: (1) src/ui/ViewportImage.tsx — extend the `overlay` render-prop callback (currently invoked at lines 625-635 passing only the full-image rect {x,y,w,h}) to also pass either the existing `frameToBuffer(fx,fy)` fn (defined line 219) or the `visible` rect from onLayout; and correspondingly extend the `overlay` prop type (lines 35 / 618-638). (2) src/modules/develop/DevelopCanvas.tsx — the MaskOverlay render-prop (lines 426-435) must forward that new frameToBuffer/visible value into a new MaskOverlay prop. (3) src/modules/develop/MaskOverlay.tsx (my file) — add the new prop to MaskOverlayProps and, in onPointerDown, convert px/py to buffer coords via it before calling sampleLinearRGB, and correct the false "render-buffer coords" comment at lines ~340-341. No safe self-contained partial exists: a rect-only mapping (bx=(px-rect.x)/rect.w*cv.width) is correct only in fit/CSS-zoom mode and would newly mis-sample in ROI-zoom mode (buffer holds just the window, not the full image), and it cannot be verified in-app. The comment alone was left unchanged because updating it without the code fix would document the bug as if intentional.

### `src/extensions/ExtensionManagerPanel.tsx`
- **What:** Built-in extensions' detail pages never show the Enable/Disable or Settings buttons because `id` is derived from target.manifest?.id and built-ins carry no manifest, so the toggle/settings buttons (gated on `id`) never render even though built-in list rows offer both.
- **Remainder:** Cross-file: the correct fix requires editing buildTarget() in src/extensions/ExtensionManagerPanel.tsx (NOT in my allowed edit list). Needed change: add an `id?: string` field to the DetailTarget interface (in ExtensionDetail.tsx) AND populate it in buildTarget for all three branches (repo branch: manifest?.id; built-in branch: builtin.id; manifest-by-id branch: manifest.id), then gate the toggle/settings buttons in ExtensionDetail.tsx on target.id instead of target.manifest?.id. I could not do the self-contained half alone: adding the interface field without buildTarget populating it would be unpopulated dead code, and switching the button gate to target.id while buildTarget leaves it undefined would regress the currently-working installed-extension buttons. A parallel agent owns ExtensionManagerPanel.tsx, so I made no change there and no partial change in my file.

### `src/modules/export/ExportPanel.tsx`
- **What:** Surface the 16-bit->8-bit degrade in the export status line so the user (not just the console) learns the delivered TIFF is 8-bit.
- **Remainder:** Cross-file remainder of finding 2, and ExportPanel.tsx is not in my allowed-edit list (owned by a parallel agent). The full fix would (1) add a numeric field such as `degradedTo8Bit: number` to the ExportResult interface in export-image.ts, (2) count each fall-through in renderOne/exportPhotos and populate it, and (3) in ExportPanel.tsx handleExport (around lines 332-336) append a note to `setStatus(...)` when result.degradedTo8Bit > 0, e.g. `; N exported as 8-bit (device can't render 16-bit)`. I did NOT add the ExportResult field in my file because with no consumer wired in the panel it would be dead code; instead I applied the self-contained console.warn. To complete: add the ExportResult.degradedTo8Bit field + counting in export-image.ts and the status-line branch in ExportPanel.tsx together in one change.

### `src/modules/export/export-image.ts`
- **What:** Graceful caller-side fallback to per-file delivery when the ZIP32 limit is hit.
- **Remainder:** Cross-file remainder outside my allowed-edit list (owned by a parallel agent). The zip.ts guard now throws a RangeError on overflow. exportPhotos() already wraps zip.add() in try/catch (lines 463-475) so it will not crash, but on overflow it emits a partial archive and marks only the overflowing photo as failed. To fully implement finding 2's suggested behavior, export-image.ts's exportPhotos should either pre-sum the encoded blob sizes and switch delivery to 'files' when the total would exceed 0xFFFFFFFF (or photos.length > 0xFFFF), or catch the ZipWriter RangeError and re-deliver the whole batch per-file with a status message, rather than continuing to accumulate into the doomed ZipWriter.

### `src/catalog/load-image.ts`
- **What:** Raising rawCacheMaxEdge never invalidates/upgrades existing cached RAW previews for Develop: the fast path (line 186) returns any cached entry because minEdge defaults to 0, so the upgrade branch (243-246) is unreachable except when an export demands more pixels.
- **Remainder:** The correct fix is cross-file and outside my edit list. It requires storing the write-time cap in the cache-entry metadata and returning it on read so the fast-path guard can distinguish 'capped small by an old setting' (should re-decode) from 'already at native resolution' (should not). CROSS-FILE REMAINDER: (1) src/raw/cache-worker.ts — persist msg.maxEdge as `cap` in handleWrite's stored record, and add `cap: number` to the `{ type: "read" }` CacheResponse variant + return it from handleRead; (2) src/raw/cache-bridge.ts — workerReadCachedPreview must return `cap` from the read response; (3) src/raw/raw-cache.ts — readCachedPreview's return type must surface `cap`. THEN in src/catalog/load-image.ts the fast-path guard at line 186 would become: accept the cached entry only when Math.max(cached.width,cached.height) >= (opts?.minEdge ?? 0) AND NOT (cached.cap < getSettings().rawCacheMaxEdge && Math.max(cached.width,cached.height) === cached.cap). A load-image.ts-only heuristic was rejected as incorrect: without the native size or stored cap, `entry.edge < currentCap` cannot tell a small-capped entry from an already-native-resolution one, so it would force a 3-8s full re-decode on every Develop open of any sub-cap-resolution photo, defeating the cache (a worse regression than the reported bug). The Preferences-hint sub-part of the finding is ALREADY FIXED: src/ui/components/PreferencesDialog.tsx:1288 already reads 'Develop edits render from this cached preview; exports larger than the cap re-decode the full RAW', which accurately describes the real behavior (and that file is outside my edit list regardless).

### `src/extensions/host.tsx`
- **What:** Cross-file remainder for finding 2: wire initPresets() into the boot sequence.
- **Remainder:** src/extensions/host.tsx is not in my allowed-edit list (a parallel agent owns it). To complete finding 2, add `initPresets` to the existing import from "@/state/presets-store" and call `initPresets();` inside initExtensionHost() alongside `initSettings();`/`initKeybindings();` (currently lines 211-212). Without this call, initPresets() is defined but never runs, so cross-window preset sync stays inactive; the self-contained part (the initPresets function itself) is already added in presets-store.ts.

### `src/modules/develop/MaskOverlay.tsx`
- **What:** Finding 1 (remainder): surface the MAX_MASKS/MAX_RETOUCH cap to the user so the canvas overlay can explain why nothing appeared, instead of the add failing silently.
- **Remainder:** Cross-file — out of my allowed-edit scope. Fully implementing this requires changing addMask/addSpot in the MaskSlice interface (mask-slice.ts) from `=> void` to a boolean/status return AND updating the callers that live in src/modules/develop/MaskOverlay.tsx: `store().addMask(mask)` inside newMaskWith (~line 320, which currently returns the new id unconditionally at ~line 321) and `st.addSpot(spot)` (~line 397) to detect rejection and raise a toast. Since MaskOverlay.tsx is owned by a parallel agent and not in my edit list, I did not change the return signature (that would break its callers). I applied only the safe self-contained part (skip the no-op pushEdit on rejection) in mask-slice.ts. Needed change in MaskOverlay.tsx: on a rejected add, show a user-facing toast ("Mask limit reached" / "Retouch limit reached") and skip selecting a non-existent mask/spot; correspondingly change MaskSlice.addMask/addSpot to return a boolean or status.

### `src/modules/develop/panels/TransformPanel.tsx`
- **What:** Finding 1 (line 44): imageAspect is derived from catalog photo.width/height instead of the decoded render buffer's sourceWidth/sourceHeight, so Upright corrections and constrained crop refits (buildInverseTransform, maxCropForTransform, computeGuidedCorrection) compute against the wrong aspect when decode orientation diverges from metadata.
- **Remainder:** The bug is real (same class as the [Image aspect from buffer] memory; CropPanel.tsx:49 shares it), but the suggested fix is NOT self-contained in the allowed file. The renderer's sourceWidth/sourceHeight exist only as local React state (sourceSize) inside the useDevelopRenderer hook, which is mounted exclusively by DevelopCanvas. TransformPanel is a sibling develop panel (contributed via panelExt in builtin.tsx; DevelopCanvas is rendered separately in DevelopView.tsx) and has no access to that hook's state. No shared store (develop-store, catalog-store) exposes the decoded source dimensions. Applying the fix requires cross-file changes outside the edit list: (1) src/hooks/use-develop-renderer.ts — write sourceSize.width/height into a store on each decoded frame (in the setSourceSize call around line 262); (2) src/state/develop-store.ts — add sourceWidth/sourceHeight (or sourceAspect) fields plus a setter to the DevelopState/ViewSlice, reset to 0 on photo change; then TransformPanel (and CropPanel) would read that store field with a photo-aspect fallback, matching DevelopCanvas.tsx:240-245. There is no safe partial change possible in TransformPanel alone: referencing a store field that does not yet exist would not compile, so no self-contained portion was applied.

### `src/raw/raw-cache.ts`
- **What:** Add file.lastModified to rawCacheKey (line 41) and bump the version prefix to invalidate stale entries.
- **Remainder:** Not achievable within the allowed edit list, and the alternative comment-fix (applied instead) is the correct resolution. Threading lastModified through the key requires editing the two read-only CONTEXT files (a parallel agent owns them): (1) src/catalog/load-image.ts:188 rawCacheKey call — has `file` in scope, would pass file.lastModified; (2) src/modules/library/import-photos.ts:616 (reimportPhotos) and :755 (preDecodeRawsForCache decodeOne) — both hold a File; (3) src/modules/library/import-photos.ts:736 (preDecodeRawsForCache skip-check) — has NO File object and deliberately avoids getFile() (import-photos.ts:711 comment: getFile reads the whole RAW off disk in Electron). To supply lastModified at :736 without a getFile would require persisting it onto CatalogPhoto, i.e. a schema change adding a `lastModified` field to src/catalog/types.ts (currently only has fileSize:number at line 19) plus setting it in buildPhoto/reimportPhotos in import-photos.ts. Making only a partial rawCacheKey signature change would break all four callers and corrupt the build, so no self-contained partial edit is safe. Chose the comment-correction path (both findings sanction it) because the size-only key is intentional: it must be computable from the CatalogPhoto record alone, and reimportPhotos already documents Reimport as the escape hatch for same-size edits.

### `src/extensions/store-ui.ts`
- **What:** The persisted per-extension update-check cache (sl_ext_updates / the `updates` store map) is never invalidated on uninstall or fresh install (only updateExtension resets it), so uninstall+reinstall can show a stale "Update available" badge for up to the 30-min TTL, and LS entries for uninstalled extensions accumulate forever.
- **Remainder:** CONFIRMED REAL, but the minimal correct fix is entirely cross-file — every call site that must be wired lives outside my single allowed-edit file (store-ui.ts). store-ui.ts (a leaf module owning the sl_ext_updates LS mirror) only exposes setUpdate; it has no clearUpdate/pruneUpdates. Adding those store methods here WITHOUT their callers would be dead code (nothing invokes them), which fails the craft bar and the "do not manufacture a change" rule. No safe self-contained partial fix exists inside store-ui.ts: age-based eviction at the readLsUpdates() seed (line 128) would defeat the deliberate design of keeping stale entries past the TTL to paint the last-known badge instantly during a refresh, and would alter behavior for still-installed extensions. store-ui.ts also cannot read the installed-extensions list at seed time (async native.plugins.list(), and importing loader would create an import cycle since loader.ts already imports store-ui.ts). CROSS-FILE REMAINDER (owned by a parallel agent — NOT edited): The clean design mirrors the existing sources.ts pattern exactly (forgetSource/pruneSources in the leaf module, wired in by callers). (1) Add two actions to store-ui.ts's ExtStoreUI store — `clearUpdate: (id: string) => void` (mirror of setUpdate: drop `id` from `updates` then writeLsUpdates) and `pruneUpdates: (installedIds: string[]) => void` (mirror of pruneSources: keep only entries whose id is in the installed set, writeLsUpdates if changed). These are the enabling half and belong in store-ui.ts, but are only non-dead once the callers below exist. (2) In src/extensions/loader.ts `uninstallPlugin(id)` (lines 273-281): call `useExtStoreUI.getState().clearUpdate(id)` alongside the existing deleteExtensionSettings(id)/persistDisabled cleanup (equivalently, add a `forgetUpdate(id)` call next to the existing `forgetSource(id)` at src/extensions/ExtensionManagerPanel.tsx line 348 inside remove()). (3) In src/extensions/loader.ts `installFromGitHub(spec)` (lines 250-271): after `await loadPlugin(manifest)`, reset the entry via `useExtStoreUI.getState().setUpdate(manifest.id, { latestTag: manifest.version, hasUpdate: false, checkedAt: Date.now() })`, exactly as updateExtension already does at loader.ts lines 348-352 (this also makes updateExtension's own reset redundant since it routes through installFromGitHub). (4) For stale accumulation, call `useExtStoreUI.getState().pruneUpdates(l.map(m => m.id))` next to the existing `pruneSources(l)` in src/extensions/ExtensionManagerPanel.tsx refresh() (line 165), or after fetching `list` in loader.ts `checkAllExtensionUpdates` (line 368). Because all four wiring points are in loader.ts and ExtensionManagerPanel.tsx (neither in my allowed-edit list), the fix is left entirely to the agent owning those files to keep the store method and its callers landing together.

### `src/state/accessibility.ts`
- **What:** Interface-level reduceMotion is applied only inside the core.accessibility extension runtime (accessibility.ts line 183 via applyAccessibility), so with the extension disabled a persisted reduceMotion:true setting stops taking effect (deactivateAccessibility even removes the sl-reduce-motion class at line 258), and settings-store.applySideEffects deliberately defers it (lines 342-343).
- **Remainder:** The bug is real, but no correct fix is self-contained in the one file I may edit (src/state/accessibility.ts). This module has NO always-on entry point: it is reached only via activate/deactivateAccessibility (extension enable/disable) and the useKeyboardCanvasEditing hook; applyAccessibility and its store subscriptions exist only while the extension is active. Therefore reduceMotion can only be re-applied while the extension is OFF from settings-store.applySideEffects, which is the read-only CONTEXT file I am forbidden to edit. CROSS-FILE REMAINDER (belongs to the agent owning src/state/settings-store.ts): in applySideEffects(s: AppSettings), apply the manual reduce-motion class, i.e. add `document.documentElement.classList.toggle("sl-reduce-motion", s.reduceMotion);` (replacing the deferral comment on lines 342-343). Then, and ONLY together with that, the manual `s.reduceMotion ||` term should be dropped from accessibility.ts line 183 so the extension only OR-s in the OS prefers-reduced-motion preference (`osWants("reduceMotion")`). I did NOT make that line-183 change here because on its own (without the settings-store counterpart) it would REGRESS behavior — reduce-motion would then never follow the manual toggle even while the extension is active. Note also: the finding's premise is partly inaccurate — reduceMotion has no standalone 'Preferences > Interface' toggle; its only UI control is in src/extensions/accessibility/AccessibilitySettings.tsx (lines 135-140), which is registered by the extension and disappears when it is disabled (builtin.tsx line 384-386), so it is not a visible no-op toggle. The real defect is a persisted setting silently ceasing to apply while the extension is off. The finding's second suggested fix (disable/annotate the Interface toggle) is thus moot and would target AccessibilitySettings.tsx, also outside my edit list.

### `src/state/catalog-store.ts`
- **What:** setCopyName also does not emit emitMetadataChange (mentioned as a 'consider' in the suggested fix).
- **Remainder:** Not part of the confirmed finding and semantically inappropriate: setCopyName is documented (lines 76-77) as display-only that never touches the file on disk, and copyName is a Safelight-internal virtual-copy distinguisher with no EXIF/XMP equivalent, so firing metadata hooks for it would be wrong. Rotation, by contrast, is a genuine orientation metadata change sidecar writers care about. Left setCopyName unchanged.

### `src/modules/develop/panels/CropPanel.tsx`
- **What:** imageAspect (line 49) is derived from photo.width/photo.height instead of the decoded render buffer's sourceWidth/sourceHeight, so on orientation-divergent decodes the aspect is transposed and computeCropForAspect / fitCropToImage / buildInverseTransform skew aspect-locked crops (violates the 'Image aspect from buffer' invariant; DevelopCanvas.tsx:240-245 and use-develop-renderer.ts:124-129 do it correctly).
- **Remainder:** Finding is REAL and confirmed, but no fix is possible within the allowed edit list. The decoded source size lives only as local React state (sourceSize) inside the useDevelopRenderer hook and is NOT published to any shared store — develop-store.ts has no sourceWidth/sourceHeight/imageAspect field, and its only setState from the hook (use-develop-renderer.ts:304) syncs as-shot WB temperature, not dimensions. CropPanel is a side panel with no canvasRef and must not call useDevelopRenderer (would spin up a second renderer bridge). A CropPanel-only edit reading a not-yet-existing store field would fail to compile or read undefined and corrupt the crop math, so there is no safe self-contained portion to apply. CROSS-FILE REMAINDER (both files outside my edit list, owned by a parallel agent): (1) src/state/develop-store.ts — add a decoded source-size field, e.g. `sourceSize: { width: number; height: number }` (default {width:0,height:0}) plus a `setSourceSize(width, height)` action in the DevelopState store. (2) src/hooks/use-develop-renderer.ts — inside the decode callback where `setSourceSize` is called (~line 262), also publish the same dims to the store, e.g. `useDevelopStore.getState().setSourceSize(image.width, image.height)`, and reset it to 0×0 on photo switch alongside the existing `setSourceSize({width:0,height:0})` at ~line 481. (3) THEN in src/modules/develop/panels/CropPanel.tsx (my file) change line 49 to read the store field with the metadata fallback, matching DevelopCanvas.tsx:240-245: `const src = useDevelopStore((s) => s.sourceSize); const imageAspect = src.width > 0 && src.height > 0 ? src.width / src.height : photo && photo.height > 0 ? photo.width / photo.height : 1;` (removing the `photo`/`photos`/`activePhotoId` lookups only if no longer otherwise needed). Note: src/modules/develop/panels/TransformPanel.tsx:44 has the identical latent bug and should get the same store-field treatment, but it is a read-only context file here.

### `src/state/develop-slices/mask-slice.ts`
- **What:** The finding's alternative sub-suggestion to make addMask / addRangeComponent return a success boolean so createMask can call commitEdit('New Mask') only when a mask was actually added.
- **Remainder:** Cross-file: that change belongs in mask-slice.ts (a read-only context file owned by a parallel agent), not in my allowed edit list. It is also unnecessary — disabling the '+ Create Mask' entry point at the cap (applied in MasksPanel.tsx) already prevents the spurious commit and the no-op arm, so no signature change is required. If pursued later: give addMask (mask-slice.ts:194) and addRangeComponent (mask-slice.ts:245) a boolean return threaded from the 'added' flag, then gate commitEdit('New Mask') in MasksPanel.createMask on that result.

### `src/modules/library/LibraryListRow.tsx`
- **What:** Identical leaking createObjectURL fallback duplicated in LibraryListRow.tsx (line 41); finding suggested extracting a shared useThumbnailUrl(photo) hook used by both files.
- **Remainder:** LibraryListRow.tsx is a read-only context file (not in the allowed-edit list) owned by a parallel agent, and a shared hook would also require a new shared module imported by both files. Cross-file remainder to apply separately: create a shared hook (e.g. src/ui/hooks/useThumbnailUrl.ts) returning photo.thumbnailUrl when present else an object URL created in useEffect and revoked on cleanup/blob change, then use it in LibraryListRow.tsx (replacing the useMemo at lines 39-43) and refactor Thumbnail.tsx to consume the same hook. Note: LibraryListRow.tsx line 6 already has a pre-existing unused `useRef` import (TS6133) unrelated to this finding.

### `src/state/keybindings-store.ts`
- **What:** Canonical fix: add altDef: "Backspace" to the photo.remove KeyAction (line 77) and drop the local Backspace fallback in use-culling-shortcuts.ts, letting matchAction own the alias (mirrors mask.delete on line 54).
- **Remainder:** keybindings-store.ts is a read-only context file owned by a parallel agent; cannot edit it. Applied an equivalent self-contained gate in the allowed file instead, which yields the same runtime behavior. Cross-file remainder: in src/state/keybindings-store.ts change the photo.remove entry to `{ id: "photo.remove", label: "Remove from catalog", category: "Library", def: "Delete", altDef: "Backspace" }`, then in use-culling-shortcuts.ts the fallback and the getBinding import can be removed.

### `src/modules/library/use-culling-shortcuts.ts`
- **What:** Register Ctrl/Cmd+A (select-all) and ArrowUp/Down (grid-row navigation) as rebindable KeyActions so they too honor Preferences, since the file comment claims all combos are rebindable.
- **Remainder:** Out of scope for this finding (framed as an alternative observation) and not implementable in the allowed file alone: it requires adding new action ids to the read-only src/state/keybindings-store.ts KEY_ACTIONS list. No change made.

### `src/project/folder-ops.ts`
- **What:** Core renamePhoto sanitization does not reject Windows-reserved characters either.
- **Remainder:** Not in allowed-edit list (parallel agent owns it). Optional cross-file alignment: renamePhoto at line ~370 does newBaseName.trim().replace(/[/\\]/g, "").replace(/[. ]+$/, ""), which strips only / \ and trailing dots/spaces but leaves : * ? \" < > | intact, so a programmatic caller bypassing RenamePhotoDialog could still hand a reserved-character name to the fs. If desired, extend that replace/validation to the same set /[:*?\"<>|]/. Not required for the dialog-level finding, which is now fixed.

### `src/ui/components/ViewMenu.tsx`
- **What:** View and Layout top-bar dropdowns close only on outside pointerdown, not on Escape, unlike DisplayTransformControl/SurroundControl which register pushEscapeHandler.
- **Remainder:** Finding is REAL and confirmed, but the fix cannot be applied within the allowed file. ViewMenu.tsx (and LayoutMenu.tsx) hold NO popover state — the open/setOpen useState and the outside-pointerdown useEffect live entirely in the shared parent component src/ui/components/TopBarMenu.tsx (lines 19, 22-29). ViewMenu just passes children into <TopBarMenu>, so there is no setOpen in scope to call from an Escape handler and no self-contained part landable in ViewMenu.tsx alone. CROSS-FILE REMAINDER (parallel agent owns this file — do NOT edit): in src/ui/components/TopBarMenu.tsx, inside the existing `useEffect` guarded by `if (!open) return;` (lines 22-29), add `import { pushEscapeHandler } from "@/ui/escape-stack";`, then within that effect register `const popEsc = pushEscapeHandler(() => setOpen(false));` and return a cleanup that both removes the pointerdown listener and calls popEsc() — mirroring DisplayTransformControl.tsx lines 28-39. Fixing it there resolves both ViewMenu and LayoutMenu at their single shared source (TopBarMenu's only consumers are ViewMenu and LayoutMenu).

### `electron/preload.cjs`
- **What:** plugins.install/uninstall sit on the unsealed safelightNative.plugins surface (preload.cjs lines 86 & 94), reachable by any extension sharing the renderer realm; a silently-installed extension's manifest permissions.network then widens connect-src on next launch because extensionConnectHosts() (main.cjs 226-237) reads ALL installed manifests, defeating the documented consent model. Finding CONFIRMED real.
- **Remainder:** The fix is inherently cross-file and cannot be completed within my sole allowed edit file (electron/preload.cjs) without breaking the build. (A) Moving install/uninstall into the one-shot `privileged` object requires coordinated changes in three files I may NOT edit: src/native/privileged.ts (capture priv.plugins and add a privilegedPlugins() accessor alongside privilegedFs/privilegedUpdates around lines 20-44), src/extensions/loader.ts (installFromGitHub line 255 `native.plugins.install(spec)` and uninstallPlugin line 280 `native?.plugins.uninstall(id)` must switch from the unsealed surface to the privileged accessor), and src/extensions/types.ts (relocate `install`/`uninstall` from the Window.safelightNative.plugins interface at lines 1158-1159 into the PrivilegedBridge interface at 1107-1111). Also main.cjs would ideally split the plugins:install/plugins:uninstall IPC handlers (lines 1313 & 1321) so the privileged channel is a distinct handler. Making ONLY the preload half (removing install/uninstall from the unsealed object) would make loader.ts's native.plugins.install/uninstall calls undefined and crash every install/uninstall at runtime — the corrupt-build outcome the hard rules forbid. (B) The alternative fix (native confirmation dialog before writing to pluginsDir) lives entirely in electron/main.cjs (installPlugin / the plugins:install handler), a read-only context file outside my edit list. No preload-only change can close the hole because the attacker path and the legitimate core store-UI path traverse the same unsealed IPC channel in the same renderer realm, so the preload cannot distinguish them.

### `src/hooks/use-develop-renderer.ts`
- **What:** Changing developMaxEdge does not take effect on reopen: the bindSource fast path (line 361) binds a still-resident GPU source uploaded under the old cap, because photoSourceKey (id:rotation) excludes maxEdge and no source-cache flush runs on the settings change; the preference hint claims 'Applies when a photo is reopened.'
- **Remainder:** Confirmed real, but every viable fix crosses out of the only editable file (use-develop-renderer.ts), and no safe self-contained subset exists here. The bind fast-path in this file cannot gate on the resident texture's maxEdge (bindSource returns only a hit boolean; the resident source's maxEdge is opaque to the main thread), so it can't be fixed from this file alone. The three cross-file fixes needed (do NOT apply — parallel agents own these files): (1) Cache-key fix: in src/catalog/load-image.ts, change photoSourceKey() [line 149] to include the develop cap, e.g. `return `${photo.id}:${photo.rotation ?? 0}:${getSettings().developMaxEdge}`;` (add a getSettings import); this makes all key consumers agree and old entries age out via LRU. Inlining a maxEdge suffix only at this file's bindSource/uploadSource/hasSource/prefetch call sites would diverge from the shared helper and break the key-equality invariant, so it must live in the helper. Note: this keys the MAIN GPU source texture only; also verify it doesn't collide with the srgb16 disk cache which uses the separate rawCacheKey(relPath,fileSize,rotation). (2) Alternatively, a source-cache flush: add a `clearSources()` (or drop-non-pinned) command to RenderBridge in src/rendering/render-bridge.ts, a handler in src/rendering/render-worker.ts that calls into the renderer, and a public method in src/rendering/webgl/renderer.ts (dropSource exists but is private, line 1466) to invalidate non-pinned cached sources; then subscribe to developMaxEdge (and highBitDepth) changes to invoke it — the existing useSettings.subscribe budget watcher in getRenderBridge() (render-bridge.ts:518) is the natural place. (3) Or simply soften the hint text in src/ui/components/PreferencesDialog.tsx:1537 from 'Applies when a photo is reopened.' to 'Applies to newly decoded photos.' The same stale hint on 'High bit-depth previews' (PreferencesDialog.tsx:1589, 'Applies when Develop is reopened.') shares the identical root cause and would be resolved by the same key/flush fix.


## Tier B fixes applied

- **`src/rendering/webgl/renderer.ts`** — dispose() now deletes the six lazily-created histogram GL resources (histTex/histTexF/histTexD textures and histFbo/histFboF/histFboD framebuffers), fixing a per-dispose leak on renderer recreation.
- **`src/rendering/webgl/renderer.ts`** — syncPipeline's sSig-changed branch now also calls this.failedPrepass.clear() alongside the existing passPrograms/prepassSigs clears, so a prepass stage whose GLSL was fixed (extension update / dev-folder reload) retries compilation instead of staying disabled for the session.
- **`src/rendering/webgl/renderer.ts`** — Added a lastUploadBpp field set in each setImage upload branch (8 for RGBA16/RGBA16F, 4 for RGBA8) and used it in estimateSourceBytes instead of inferring from haveNorm16, so plain 8-bit bitmap sources are no longer double-counted (4 B/px, not 8) in the LRU source-cache budget. The now-unused `linear` parameter of estimateSourceBytes was dropped and its single caller updated.
- **`src/rendering/webgl/renderer.ts`** — readDownscaledPixels now overrides uViewport to (0,0,1,1) alongside the existing crop/transform identity overrides and restores it from this.roi afterward, so upright/line-detection readback analyzes the whole frame instead of the last frame's zoom window when a viewport ROI is active.
- **`electron/main.cjs`** — F5 (protocol handler weak prefix): added a contains(base, child) helper (path.relative + reject ..'/absolute) and replaced the trailing-separator-less startsWith checks in resolveRequestPath (DIST) and the /__plugins__/ branch, so a prefix-sharing sibling dir (e.g. plugins2 vs plugins) no longer passes containment.
- **`electron/main.cjs`** — F1 (installPlugin tar traversal + weak dest prefix): tar-entry filter now rejects any name containing a backslash (a Windows separator that bypassed the '/'-only .. check) or a normalized .. segment; replaced dest.startsWith(target) and target.startsWith(pluginsDir()) with the contains() containment check, closing cross-extension write escape.
- **`electron/main.cjs`** — F4 (missing validRepo on release paths): added `if (!validRepo(repo)) throw new Error("Bad repository")` to fetchReleases and installRelease, matching fetchRepoMeta/fetchReadme/fetchManifestVersion, so a crafted repo string can't retarget the GitHub API path.
- **`electron/main.cjs`** — F6 (empty registry index blanks store): searchExtensions now requires `index && index.length` before serving the registry, and fetchRegistryIndex no longer caches/returns an empty items array (returns last-good or null), so an empty/all-malformed registry.json falls back to live GitHub search instead of blanking the store for the 1h TTL.
- **`electron/main.cjs`** — F7 (search-cache.json unbounded growth): the search diskCache validateEntry now drops rows older than SEARCH_TTL_MS on load, and persistSearchDisk prunes expired entries from the live map before serializing; scoped to the search cache only, leaving the shared diskCache helper and og/icon caches unchanged.
- **`electron/main.cjs`** — F2 (child windows lack window-open handler): extracted the window.open allow/deny policy into windowOpenHandler and registered it inside the central app.on('web-contents-created') hook so every webContents — detached-module children and their children — is covered (will-navigate only guards in-page navigation, not a new window's initial load). Removed the now-redundant per-window setWindowOpenHandler in createWindow; the hook is registered at startup and fires during window construction, before createWindow proceeds.
- **`src/project/folder-ops.ts`** — movePhotos now handles virtual copies: it filters copies out of the on-disk move set (a copy's relPath points at the master's file), and when a master moves it carries every copy's folder/relPath/directoryHandle/fileHandle along in the same relocatePhotos call, mirroring renamePhoto. A copy dragged without its master is skipped so the master's file is never moved out from under it.
- **`src/project/folder-ops.ts`** — copyDir no longer skips dot-entries; in the FSA build a moved/renamed folder's hidden user files now get copied before the recursive source delete instead of being destroyed. The project's .safelight working dir lives at the project root and is never a moved subfolder or inside one, so it can't be dragged through copyDir.
- **`src/project/folder-ops.ts`** — moveFolder and renameFolder now probe the destination with a new dirExistsRel helper (fs.exists in native, getDirectoryHandle-without-create in FSA) and bail before moveOnDisk on collision, preventing the FSA copyDir/copyFile {create:true} path from silently merging into and overwriting a same-named folder/files. Bail is a no-op + console.warn (matching this file's diagnostic style); see skipped for the user-visible-error remainder.
- **`src/project/folder-ops.ts`** — movePhotos now best-effort moves each moved photo's <file>.safelight.json sidecar from the old folder to destRel (guarded by existsRel), so the sidecar travels with its image as it does in renamePhoto, instead of being orphaned in the source folder.
- **`src/project/folder-ops.ts`** — exportPhotoData now skips photos with copyOf set. Because a virtual copy shares the master's filename and directoryHandle, its sidecar path collided with the master's, so exporting a master+copy selection silently overwrote one's ratings/edits with the other's while counting both; skipping copies stops the clobber and stops the inflated success count. See skipped for the user-facing 'copies were skipped' remainder.
- **`src/ui/components/PreferencesDialog.tsx`** — Finding 1 (line 1285): Corrected the 'Cached preview resolution' hint. Verified Develop calls loadPhotoImage with no minEdge (use-develop-renderer.ts:384) so it accepts the cap-limited cached preview (load-image.ts:186) and shows 'Cached WxH'; only export passes minEdge (export-image.ts:244) to bypass the cap. New hint: 'Long-edge cap of cached previews. Develop edits render from this cached preview; exports larger than the cap re-decode the full RAW.'
- **`src/ui/components/PreferencesDialog.tsx`** — Finding 3 (line 1424): StoredCatalogsField's cleanup-only mountedRef effect left the ref permanently false after StrictMode's dev double-mount, so del()'s mountedRef.current guards (lines 1461/1464) never fired and a deleted row stuck at 'Deleting...'. Changed the effect to set mountedRef.current = true in the body and clear it in cleanup.
- **`src/ui/components/PreferencesDialog.tsx`** — Finding 4 (line 194): Search index had drifted from rendered controls (sectionMatches only checks items/keywords, so absent controls report 'No settings match'). Added 'Show photos in subfolders' to Library items, 'Only verified extensions' to Extensions items, and 'TIFF bit depth' plus a 'tiff' keyword to the Export section. Left the longer-term 'derive index from rendered fields' refactor for a separate change per the finding.
- **`src/ui/components/PreferencesDialog.tsx`** — Finding 5 (line 2035): Update-check hint claimed the app checks 'once on launch', but UpdateBanner.tsx re-polls every 3h via setInterval (APP_UPDATE_POLL_MS) and its own comment calls launch-only the old behaviour. Changed the hint to 'checks the GitHub releases API on launch and every few hours while the app is open'.
- **`src/rendering/render-worker.ts`** — renderThumbnail/renderThumbnailFromSource stale-stage-bag leak: wrapped tr.render() in try/finally inside finishThumbRender so the global contributed-params bag is restored even when render() throws, preventing subsequent thumbnails from rendering with the wrong photo's stage uniforms.
- **`src/rendering/render-worker.ts`** — capture case hang: wrapped the render body in try/catch that restores lastParams and responds with the blank 1x1 fallback bitmap (carrying reqId) on any throw, so the bridge's captureResolvers entry always settles and an awaiting extension capture() never hangs.
- **`src/rendering/render-worker.ts`** — analyzeUpright silent break: the handler now always responds — a zero-result upright ({straighten:0,perspectiveV:0,perspectiveH:0}) on the !renderer/!pixels paths — so RenderBridge.computeUpright's (timeout-less) promise settles and TransformPanel's await no longer hangs with buttons stuck disabled.
- **`src/extensions/devtools/DevToolsPanel.tsx`** — Console autoscroll now keys the effect on the newest entry id (plus length), not just shown.length, so it keeps following new output once the log buffer hits its MAX_ENTRIES cap and trims one old entry per new one.
- **`src/extensions/devtools/DevToolsPanel.tsx`** — readWebGLInfo no longer leaks a WebGL context per System-tab mount/Refresh: the result is cached module-level (values are static per session) and the probe context is released via WEBGL_lose_context, so repeated refreshes can't evict the app's real render context.
- **`src/extensions/devtools/DevToolsPanel.tsx`** — StorageTab's Save now surfaces localStorage.setItem failures (e.g. quota exceeded) via an inline saveError shown next to the Save button, mirroring NativeTab's note pattern, instead of silently swallowing the exception; cleared on success and on key selection.
- **`src/extensions/loader.ts`** — applyEnablement's external-enable path now runs the same trust kill-switch guard as loadExternalPlugins (bannedReasonForManifest -> flagBannedExtension + console.warn + early return) before loadPlugin, so a banned-but-installed extension can no longer be re-activated by toggle-off/on or by the cross-window storage listener.
- **`src/extensions/loader.ts`** — Extracted parseDisabled(raw) with the Array.isArray + string-filter validation and made both loadDisabled() and initEnablement's storage listener use it, so a malformed cross-window value can no longer throw in the listener or poison useDisabledExtensions with a non-array; the two DISABLED_KEY readers now share one parse path and cannot drift.
- **`src/modules/library/import-photos.ts`** — reimportPhotos (line ~661) now recomputes the canonical rotation from the freshly parsed EXIF instead of reusing the stale photo.rotation: it peels the manual turns off the OLD EXIF (normalizeRotation((photo.rotation ?? oldExifRot) - oldExifRot)) and re-adds the new EXIF portion (normalizeRotation(manual + orientationToRotation(exif.orientation))). Fixes a file whose on-disk EXIF orientation changed (e.g. old tag 90 externally rotated upright with tag cleared) coming back sideways, and removes the previously-dead '?? orientationToRotation' fallback.
- **`src/modules/library/import-photos.ts`** — rebuildThumbnails (line ~568) now sets decodeError: undefined on a successful rebuild, matching repairMissingPreviews (line 520) and reimportPhotos (line 678), so a previously-failed photo no longer keeps a stale persisted 'decode failed' marker that would resurface the ⚠ tile if the preview blob is later dropped/reloaded.
- **`src/modules/develop/MaskOverlay.tsx`** — chooseRetouchSource: scaled the x component by imageAspect in the painted-extent hypot (line ~743) so `rad` handed to findHealSource covers the true painted extent on landscape images and the heal-source search rings can't overlap the painted region.
- **`src/modules/develop/MaskOverlay.tsx`** — subErase: subscribed maskCompMode via useDevelopStore (mirroring the reactive brushErase) instead of reading it with a non-reactive store().getState, so toggling add/subtract with the cursor stationary re-renders the brush ring colour/dash immediately.
- **`src/extensions/ExtensionDetail.tsx`** — Manifest icon (which per ExtensionManifest.icon may be repo-relative) was used raw as an <img> src, 404ing against the app origin and short-circuiting the og/avatar fallback. Now resolved via resolveUrl(icon, 'img', repo, branch) into an ordered candidate list (manifest icon -> ogImageUrl -> ownerAvatarUrl); on img error the tile advances to the next candidate (iconIdx state, reset when the candidate set changes) instead of hiding via display:none.
- **`src/extensions/ExtensionDetail.tsx`** — When the native github bridge is absent (plain browser / older Electron), loadRepoMeta/loadReadme no-op so meta/readme stayed undefined and the README area showed 'Loading...' forever. Added a hasGithubBridge check (window.safelightNative?.github) and a 'No repository details available in this build.' fallback branch before the loading/error branches.
- **`src/extensions/ExtensionManagerPanel.tsx`** — Browse installed-detection: rebuilt installedRepos from the installed list via repoFor(m)?.toLowerCase() (filtered non-null) instead of only readSources(), so an extension that self-declares manifest.repository (which repoFor prefers) is now detected as installed and no longer shows a duplicate Install button. Removed the now-unused readSources import.
- **`src/extensions/ExtensionManagerPanel.tsx`** — install(): now calls rememberSource(manifest.id, repo) whenever the parsed repo is non-null (not only when fromSearch is present), so custom 'owner/repo' imports keep update checks and installed-detection. Guarded with !installSpec.includes('#') so a #branch pin (which tracks a ref, not releases) is not recorded as a release-trackable source. Search installs still get remembered (their spec has no '#').
- **`src/extensions/ExtensionManagerPanel.tsx`** — Featured shelf: added a reactive subscription const verified = useTrust((s) => s.list.verified) and filter the shelf against it (mirroring isVerifiedIn's trim+lowercase) instead of the non-reactive isVerified(getState), so the shelf recomputes when the trust registry loads asynchronously on first run / cleared localStorage. isVerified import retained (still used non-reactively inside install()).
- **`src/state/develop-store.ts`** — moveHistory (undo/redo) now regenerates the edited grid thumbnail and re-emits the edit to extensions via emitEditCommit after restoring a snapshot, mirroring commitEdit, so the Library thumbnail and XMP sidecars no longer stay stale at the pre-undo look. Kept fire-and-forget (void) to preserve the synchronous undo/redo contract, since making them async would require changing the out-of-scope caller use-keyboard-shortcuts.ts.
- **`src/state/develop-store.ts`** — Narrowed applyPreset's parameter type from Partial<DevelopParams> to DevelopParams, removing the footgun where a true partial would silently reset every omitted core adjustment (including temperature to 6500K). The sole caller PresetsPanel already passes fully-merged params via effective(), so no runtime behavior changed and no cross-file edit was needed.
- **`src/state/develop-store.ts`** — loadEdit now clears transient mask/retouch tool state (activeTool, selectedMaskId, selectedComponentId, selectedSpotId, hoveredMaskId) via a new RESET_TOOL_SELECTION const spread into both branches, so switching photos no longer leaves a dangling selection or an armed tool pointing at the previous photo's objects.
- **`src/ui/ViewportImage.tsx`** — Zoom gesture no longer clears when an unrelated key is released while Space/Ctrl/Meta is still held: the window keydown/keyup listeners now maintain a Set of held gesture keys and call setCanvasZoomGesture(held.size > 0) instead of clearing on any keyup that lacks a modifier.
- **`src/ui/ViewportImage.tsx`** — Added onPointerCancel handler on the frame div that clears downRef, dragging, and the pending pan RAF, and delivers onPickDrag.onUp() — so an interrupted pan/pick (touch cancel, capture loss, OS gesture) no longer leaves the image panning on hover or the picker stuck active.
- **`src/ui/ViewportImage.tsx`** — Eyedropper pick paths (handleClick onPick and onPointerDown onPickDrag.onDown) now guard with pointOnImage the same way the zoom path does, so clicks/presses on the letterbox surround are ignored instead of forwarding out-of-image buffer coordinates to samplers.
- **`src/extensions/devtools/dev-folder.ts`** — loadOne: wrapped mod.activate() in try/catch that calls unregisterExtension(manifest.id), URL.revokeObjectURL(url), then rethrows — so a throwing activate() no longer leaks the blob URL (previously never revoked/stored in blobUrls) and no longer leaves partially-registered contributions behind. Matches the file's two existing revoke-and-rethrow blocks.
- **`src/extensions/devtools/dev-folder.ts`** — scanDevFolder: added module-level `scanGen` counter, captured `const gen = ++scanGen` at entry, and bail (return without setState/loadOne) whenever `gen !== scanGen` after each await — after fs.exists(rootManifest), after loadOne in the root branch, in the fs.list catch, at the top of the subfolder loop (guards each loadOne/exists iteration), and before the final setState. A folder change or Clear arriving via onExtSettingChange mid-scan now supersedes the stale scan instead of racing the newer scan's unloadAll and re-activating old-folder extensions.
- **`src/extensions/registry.ts`** — unregisterProcessingStage now checks stage ownership via useRegistry.getState() BEFORE calling unregisterStageParams(id); a non-owner call returns early, so a stranger can no longer strip param descriptors off another extension's still-registered stage.
- **`src/extensions/registry.ts`** — registerExportProcessor now preserves an existing id's order on re-registration (s.exportProcessors[c.id]?.order) and derives new orders from a monotonic max+1 over existing processors instead of Object.keys(...).length, so re-registration no longer moves a processor to the chain end and an unregister-then-register cycle can no longer mint a colliding order index (fixes drift in runProcessors and ExportPanel ordering).
- **`src/rendering/render-bridge.ts`** — dispose(): removed the dead this.post({cmd:"dispose"}) call. It ran after this.disposed=true, and post() early-returns when disposed, so the worker dispose message was never sent. Now relies on worker.terminate() (which tears down the worker and its GL context synchronously); added a constraint comment noting a dispose message would be preempted by terminate().
- **`src/rendering/render-bridge.ts`** — getRenderBridge()/disposeRenderBridge(): captured the previously-discarded useSettings.subscribe() unsubscriber into a new module-level unsubSettings (mirroring unsubStages/unsubPipeline) and call+null it in disposeRenderBridge(), so each dispose/recreate cycle no longer leaks a live settings listener.
- **`src/extensions/dock.tsx`** — Finding 2 (bringToFront, ~line 473): added early-return `if (s.zOrder[s.zOrder.length - 1] === id) return;` so clicking an already-frontmost floating panel no longer calls commit()/markLayoutCustom()/scheduleSave() and no longer flips an active preset/user layout to "Custom" with a spurious save.
- **`src/extensions/dock.tsx`** — Finding 1 part A (startHeaderDrag float drop): the grab offset ox and captured float width were read from getBoundingClientRect() in visual px but stored as layout-px FloatState (CSS left/top/width). Divided ox and width by the uiScale zoom at capture, used frameLocalPoint(c, clientX, clientY) for the container-relative drop point, and divided the c.width/c.height clamp bounds by the zoom, so a float drops under the cursor and at correct width at uiScale != 1.
- **`src/extensions/dock.tsx`** — Finding 1 part B (startResize): the pointer delta (ev.clientX - startX) is visual px but was added directly to a layout-px width, making rail/float resizes move uiScale-times faster than the pointer. Divided the delta by the uiScale zoom (mirrors ModalWindow.tsx / frame-point.ts) so width tracks the pointer 1:1.
- **`src/extensions/dock.tsx`** — Finding 1 part C (DragOverlay): the drop-line, new-rail strip, and drag-ghost fixed-position coordinates come from hitTest getBoundingClientRect()/raw clientX-Y (visual px) but render as CSS left/top/width/height on position:fixed elements that the <body> zoom scales as layout px, so indicators drifted at uiScale != 1. Divided every rendered coordinate by the uiScale zoom (small literal offsets kept in layout space).
- **`src/project/project-storage.ts`** — ProjectStorage.open: the `!built` early return now bumps `onProgress?.(++newDone, newTotal)` before returning null, matching the abort/success/catch paths so a photo buildPhoto returns null for no longer leaves importDone permanently short of importTotal.
- **`src/project/project-storage.ts`** — deletePhoto now opens .safelight/blobs/ via a non-creating getDirectoryHandle (wrapped in try/catch) when the session-local handle is null, so per-photo blobs written in earlier sessions are cleaned up on delete; projects that never used blobs still get no empty dir created.
- **`src/project/project-store.ts`** — Line 201 folder-filter key: was keyed by handle.name (folder basename), so two projects sharing a folder name on different paths collided. Now keyed by nativePathOf(handle) ?? handle.name (the same identity recent.ts uses), and restoreActiveFolder is passed that key. Added nativePathOf to the existing ./native-fs import.
- **`src/project/project-store.ts`** — Line 271 straggler flush: `buf = []` ran unconditionally before the generation check, so the following flush() always drained an empty buffer. Reordered to `if (gen !== openGen) { buf = []; return; }` then flush(), so the discard happens only on cancellation and flush actually drains photos buffered since the last frame.
- **`src/project/recent.ts`** — Connection leak: openDB() connections were never closed (addRecentProject opened one per trimmed entry via removeRecentProject). Added `db?.close()` in a finally at each call site (addRecentProject, listRecentProjects, removeRecentProject), extracted a file-private deleteEntry(db, id) helper, and reused the already-open db in the trim loop instead of reopening per entry.
- **`src/project/recent.ts`** — migrateLegacy re-armed forever: it now consumes the legacy sources after a successful migration — localStorage.removeItem(LEGACY_LS_PATH) in the Electron path and delete of the legacy store's 'last' record in the browser path — so removing every recent stays removed instead of resurrecting the old last project on the next welcome-screen load.
- **`src/modules/export/export-image.ts`** — Wrapped the renderOne call in exportPhotos (line ~449) in a per-photo try/catch mirroring renderPhotosToBlobs, so a throw from one photo is caught, pushes photo.filename to failed[], and the loop continues — the batch, the failed[] report, and the final ZIP download (line 479) all survive a single bad photo instead of the whole export aborting.
- **`src/modules/export/export-image.ts`** — Added a console.warn at the 16-bit-TIFF float-capture fall-through in renderOne (line ~301): when captureFloatFrame() returns null (device can't render to a float target) the export previously dropped to 8-bit with no signal at all despite the UI promising full 16-bit precision; now it emits a per-photo warning naming the file. Applied only the self-contained signal since the fuller status-line surfacing requires editing the parallel-owned ExportPanel.tsx (see skipped).
- **`src/modules/export/ExportPanel.tsx`** — loadPreset now applies preset.colorSpace: added a per-session colorSpace state seeded from getSettings().exportColorSpace (ColorSpaceId), set it in loadPreset from preset.colorSpace, and use it in both savePreset and handleExport's ExportSettings instead of re-reading getSettings().exportColorSpace. A preset saved with e.g. Adobe RGB now exports in that space instead of silently using whatever Preferences currently says.
- **`src/modules/export/ExportPanel.tsx`** — Changed the failure status copy from '... could not be decoded.' to '... could not be exported.' since exportPhotos also pushes filenames into failed[] on delivery errors (ZIP add / folder write), which the decode-specific wording misreported.
- **`src/modules/export/zip.ts`** — Set general-purpose flag bit 11 (EFS, 0x0800) in both the local file header (offset 6) and central directory header (offset 8) when a filename contains any byte >= 0x80, so spec-honoring extractors decode UTF-8 photo names correctly instead of as CP437 mojibake. Added per-entry utf8 flag on ZipEntry and a hasHighByte() helper.
- **`src/modules/export/zip.ts`** — Guarded the ZIP32 32-bit size/offset and 16-bit entry-count fields in add(): throws RangeError when a single file exceeds 4 GiB, when the running archive offset would exceed 0xFFFFFFFF, or when entries reach 0xFFFF, instead of silently writing wrapped/truncated fields and producing a corrupt archive.
- **`src/catalog/load-image.ts`** — bitmapToFloat leaked a fresh WebGL2 context per call (once per RAW slow decode via rawColorMatchesPreview), so a batch prefetch over many RAWs could exhaust the browser's ~16 live-context limit and evict a live canvas. Added gl.getExtension("WEBGL_lose_context")?.loseContext() after the framebuffer/texture deletes to release the transient context immediately. CPU-side readback utility only — no WebGL render-path pixel change; uses an idiom already present at DevToolsPanel.tsx:755, no casts.
- **`src/catalog/types.ts`** — Removed the 4-line cap in normalizeGuidedLines (dropped `if (out.length >= 4) break;`) so all persisted guided-upright lines survive reload, matching GuidedOverlay's unbounded line drawing and computeGuidedCorrection's full-list iteration; there is no shader/count constraint requiring a cap.
- **`src/catalog/types.ts`** — normalizeCurve now validates each persisted point (typeof number + isFinite) and clamps x/y to 0..1, falling back to DEFAULT_TONE_CURVE if any point is corrupt, matching the defensive posture of the sibling normalizers so NaN/string/out-of-range data can no longer flow into the tone-curve LUT.
- **`src/modules/develop/preset-summary.ts`** — Added luminanceNRShadows and luminanceNRHighlights to PARAM_LABELS (adjacent to luminanceNRContrast, matching DetailPanel slider order), so summarizePreset/presetFields/buildPartialParams now cover these two Detail-panel sliders instead of silently dropping them from presets and tooltips.
- **`src/modules/develop/preset-summary.ts`** — Excluded straighten from the scalar-field loop in presetFields via an early continue, so it is offered solely inside the per-image 'Crop & transform' geometry field (which already carries it in keys) and is no longer emitted as a standalone pre-checked global scalar; kept its PARAM_LABELS entry for tooltip display of older presets.
- **`src/modules/develop/panels/PresetsPanel.tsx`** — Update dialog (Update with current settings) discarded result.name: updatePreset never writes name. Added a renamePreset(updating.id, result.name) after updatePreset in the updating PresetSaveDialog onSave, guarded by the same case-insensitive collision check the rename dialog uses (excluding the preset's own id), so an in-dialog rename is now honoured unless it clashes with a different preset.
- **`src/modules/develop/panels/PresetsPanel.tsx`** — Import bypassed the name-collision flow: both handleImport paths called addPreset directly, silently creating duplicate-named presets. Routed both the native parseSafelightPreset path and the extension-importer path through commitSave, which fires the Overwrite / Save-as-new dialog on a case-insensitive name clash (types line up: PendingSave.group defaults to "", paramBag omitted for importer results). The immediate applyPreset preview calls are preserved.
- **`src/modules/develop/preset-io.ts`** — parseSafelightPreset now validates the parsed shape at the import boundary: new PARAM_KINDS table + sanitizeParams() keep only known DevelopParams keys whose runtime value matches the expected kind (finite number for scalars, string for uprightMode, object/array for complex keys), so a malformed scalar (e.g. exposure:"abc") or params-as-string can no longer spread through normalizeParams into the renderer as NaN uniforms. Name is guarded to a non-empty string (falls back to filename; empty string previously passed the ?? fallback), group is kept only when a string, and paramBag only when a plain object.
- **`src/modules/develop/preset-io.ts`** — pickPresetFile now wires input.oncancel = () => resolve(null) alongside onchange, so cancelling the file picker resolves null per the documented "or null if cancelled" contract instead of leaving handleImport's promise permanently pending.
- **`src/modules/develop/panels/ColorGradingPanel.tsx`** — Double-click reset on a color wheel: skip applyVec + commit on the second click (e.detail > 1) in onPointerDown and onPointerUp, so the reset isn't preceded by a stray commit of the click position that would hijack the next undo (commitEdit has no no-change dedup). onPointerUp now takes the pointer event to read e.detail.
- **`src/modules/develop/panels/ColorGradingPanel.tsx`** — Emptying the hue/sat text input no longer snaps the wheel to 0: guard the onChange apply with e.target.value.trim() !== "" (Number("") === 0 previously passed the Number.isFinite check), so an emptied field applies nothing until a real number is typed or blur restores the current value.
- **`src/state/keybindings-store.ts`** — Added altDef: "Ctrl+Shift+{" to develop.surroundDarker and altDef: "Ctrl+Shift+}" to develop.surroundLighter. On US layouts, Shift+[ yields e.key "{" (and Shift+] yields "}"), so comboFromEvent emits "Ctrl+Shift+{"/"Ctrl+Shift+}", which never matched the bracket-based def. The alias is honored by matchAction (line 280) while the action is unbound, mirroring the existing brush.featherDown/featherUp pattern.
- **`src/state/keybindings-store.ts`** — findConflicts() now adds a second scan entry (same id, same category, combo = altDef) for every built-in action that has an altDef and no override, so live alias combos (e.g. Ctrl+Y for develop.redo while unbound) collide visibly in the Shortcuts UI instead of being silently resolved by matchAction's KEY_ACTIONS ordering. Extension actions have no altDef and are unaffected.
- **`src/state/thumbnail-loader.ts`** — pump() now re-checks gen === myGen after await loader(id) resolves before pushing to pending, so a blob resolved after a project swap is not merged into the new catalog under the old id (mirrors reloadThumbnail's guard).
- **`src/state/thumbnail-loader.ts`** — Removed the running=false force-reset from setThumbnailLoader and added runningGen tracking: pump's finally only clears running when runningGen === myGen (it still owns the flag), preventing a stale pump from clobbering a fresh pump's running state and starting concurrent pumps. On release it re-kicks pump() if the queue is non-empty so requests the new project queued behind the stale pump still drain.
- **`src/state/presets-store.ts`** — Wrapped saveToStorage's localStorage.setItem in try { ... } catch {}, matching settings-store and keybindings-store, so a quota failure no longer throws from inside the zustand set() updater and aborts the state update.
- **`src/state/presets-store.ts`** — Added exported initPresets() that listens for the 'storage' event on 'safelight-presets' and calls usePresetsStore.setState({ presets }) with the parsed list (typed as Preset[]), mirroring initKeybindings(), so detached windows stay in sync. NOTE: still needs a one-line wire-up at boot in the out-of-scope file src/extensions/host.tsx (see skipped).
- **`src/state/develop-slices/mask-slice.ts`** — Finding 2: updateMask now deletes patch keys whose value is undefined (Object.entries + delete) instead of spreading them, so clearing a mask block (e.g. hsl/toneCurve via applyOwnedValues clear mode) no longer leaves an own enumerable `hsl: undefined` property that structured-clone/IndexedDB persists as an explicit undefined key. Mirrors updateMaskBag's undefined-deletes semantics. No render-path change (mask.hsl reads undefined either way).
- **`src/state/develop-slices/mask-slice.ts`** — Finding 1 (self-contained part): addMask and addSpot now capture whether the MAX_MASKS/MAX_RETOUCH cap accepted the add and only call pushEdit when it did, so a cap-rejected gesture no longer fires a redundant no-op edit-update broadcast.
- **`src/modules/develop/panels/TransformPanel.tsx`** — applyUpright now captures the previously-selected upright mode and, in a new catch, restores uprightMode on a failed/rejected analysis (logging via console.error, the codebase's convention for surfacing render-worker failures per use-develop-renderer.ts:211). This stops the unhandled promise rejection and prevents the mode button from claiming a correction that was never applied.
- **`src/ui/components/CurveEditor.tsx`** — Added `if (e.button !== 0) return;` at the top of handlePointerDown so right- and middle-clicks on the curve canvas no longer add/drag control points, matching Histogram.tsx's onPointerDown button filter.
- **`src/ui/components/CurveEditor.tsx`** — Added a local `editing: { field: 'in' | 'out'; raw: string } | null` buffer for the In/Out number fields (mirroring Slider.tsx's `editing` state): each field now shows its raw text while focused, seeds the buffer onFocus, applies the parsed value onChange, and clears the buffer + commits onBlur, so a field can be cleared or hold intermediate text instead of snapping back to the rounded value on every keystroke.
- **`src/hooks/use-loupe-renderer.ts`** — Loupe now applies core.hsl style prefs via renderer.setHslStyle in the load effect (mirroring export-image.ts), so it stays pixel-consistent with Develop/export when Preferences HSL hueRange/smoothness differ from 100. Added the getExtSetting import.
- **`src/hooks/use-loupe-renderer.ts`** — Added a .catch to the load-and-render Promise.all chain (guarded on cancelled) that logs the failure and calls setLoading(false), so a rejecting loadPhotoImage/loadSavedEdit no longer leaves the Loupe spinner stuck forever.
- **`src/raw/libraw-wasm-adapter.ts`** — libraw open() transfers/detaches the caller's ArrayBuffer to its worker, killing decode.ts's in-house TIFF/CFA fallback (it ran on a detached buffer). Fixed at both open() sites (extractColorTemperature and decodeRawFloatViaLibRaw) by passing a copy via new Uint8Array(buffer.slice(0)) so the caller keeps its original buffer.
- **`src/raw/libraw-wasm-adapter.ts`** — Inferred-dimensions recovery used Math.round(totalPx/w) with tolerance |w*h - totalPx| <= w, allowing w*h > pixel count so the copy loop read past the pixels array (NaN rows). Changed to Math.floor(totalPx/w) with guard totalPx - w*h < w, guaranteeing w*h <= available pixels.
- **`src/raw/raw-cache.ts`** — Corrected the header comment (lines 9-10) that falsely claimed the cache key includes lastModified and that 'any change to the source file automatically misses the cache'. The comment now states the real contract: key = relPath + fileSize + baked rotation (derived from the catalog record so the prefetch skip-check needs no getFile), a size-changing edit misses automatically, and a same-size in-place edit stays stale until Reimport. This is the comment-correction alternative both findings explicitly offer; size-based invalidation is the deliberate design (import-photos.ts:711 depends on the key being computable without getFile).
- **`src/extensions/devtools/DevSettings.tsx`** — Rewrote the Preferences ▸ Developer Tools folder hint to document both scan modes — a single extension's folder (root safelight.json) or a parent whose immediate subfolders are each one — matching scanDevFolder's root-manifest-preferred behavior and the Extensions ▸ Dev tab copy, instead of only the parent-folder mode.
- **`src/extensions/pipelines.ts`** — resolveActivePipeline no longer collapses a glsl-less contribution with skipBaseCurve:true to BUILTIN_RESOLVED; it now resolves { id, glsl: null, skipBaseCurve: true, sig: `${id}\n` } so the declared flag survives and the renderer swaps programs correctly, while a glsl-less contribution without the flag still falls back to BUILTIN_RESOLVED. Non-pixel data-resolution fix (built-in default and existing glsl pipelines unchanged).
- **`src/extensions/ui-kit.tsx`** — NumberInput onChange now bails out early when the input is empty (e.target.value === "") before the Number()/isFinite check, so clearing the field no longer fires onChange(0) and snaps the controlled value back to 0 mid-edit; the existing isFinite guard already discards NaN badInput from a lone '-'.
- **`src/extensions/builtin.tsx`** — Corrected the Effects extension description from "Vignette, grain and dehaze." to "Vignette and grain." — EffectsPanel renders only vignette and grain sections (reads s.params.vignette / s.params.grain), and the panel's Reset scope on line 425 resets only ["vignette", "grain"]; dehaze is a Basic-panel param (reset on line 421).
- **`src/modules/library/tiff-image.ts`** — Moved the absurd-dimensions memory guard ahead of UTIF.decodeImage. It now rejects when best (ifdArea of the selected largest page, read from IFD tags t256/t257) exceeds MAX_DECODE_PIXELS before decodeImage allocates the full page, using the already-computed best value. Kept the post-decode !width/!height check for pages without dimension tags.
- **`src/modules/library/netpbm.ts`** — P1 (plain PBM) now reads one non-whitespace character per pixel via a new readBit helper (skips whitespace/# comments, accepts only '0'/'1', else fails), instead of readToken consuming a packed row like "0110" as a single token and misparsing it as one white pixel. Also tightened the ASCII branch comment to note P1 is per-character bits while P2/P3 are whitespace-separated decimals.
- **`src/modules/develop/DevelopCanvas.tsx`** — Added an unmount-cleanup useEffect that cancels the pending pushCrop requestAnimationFrame and clears pendingCrop.current, so a crop write queued mid-drag can no longer fire after unmount and stamp the previous photo's crop into the newly loaded photo's params. Non-pixel lifecycle fix; does not touch the WebGL render path.
- **`src/extensions/Markdown.tsx`** — Fixed inline link regex (line 36) that forbade ']' in link text, breaking the common badge-in-link construct [![alt](img)](href). The old /\[([^\]]+)\].../ captured '![alt' as link text and 'img' as the href, leaving '](href)' as literal text. Replaced with /\[((?:!\[[^\]]*\]\([^)\s]+\)|[^\]])+)\]\(([^)\s]+)(?:\s+"[^"]*")?\)/y, which permits one nested image in link text; the existing recursive parseInline(m[1]) then renders the inner image inside the <a>. Plain images (image rule runs first, anchored on '!') and normal links are unaffected. No render-path/pixel change; tsc --noEmit adds zero new errors.
- **`src/rendering/thumbnail-renderer.ts`** — The cached main-thread histogram renderer singleton never recovered from WebGL context loss, so after a GPU reset every Library histogram silently came back blank until app restart. Added a webglcontextlost listener on the canvas getHistCtx() creates: on loss it disposes the dead WebGLRenderer and nulls histCtx so the next renderPhotoHistogram call rebuilds a fresh context. histDead is left untouched (it remains the latch for a genuine 'WebGL2 unsupported' throw, which won't recover on retry). Uses only public APIs (WebGLRenderer.dispose() and the canvas DOM event); no render/pixel-path change.
- **`src/state/catalog-store.ts`** — rotatePhotos now calls emitMetadataChange with the rotated photo records (mirroring commit()'s ctx shape: { photos, getEditState }) after the Promise.all and before the state set, so extension onMetadataChange hooks (e.g. XMP sidecar writers) receive rotations like every other metadata change.
- **`src/modules/develop/panels/MasksPanel.tsx`** — Disabled the '+ Create Mask' button when masks.length >= MAX_MASKS (new atMaskCap flag + disabled attr + 'Mask limit reached' tooltip, matching sibling panels' disabled:cursor-default disabled:opacity-40 convention). This blocks both the range-mask path that pushed a spurious empty 'New Mask' history entry when addMask no-ops at the cap, and the geometric path that silently armed a tool that no-ops on gesture end at the cap.
- **`src/ui/components/Thumbnail.tsx`** — Replaced the leaking useMemo (which called URL.createObjectURL on photo.thumbnailBlob with no matching revoke, leaking one object URL per virtualized cell remount) with a useEffect that creates the fallback object URL, stores it in state, and revokes it on cleanup/blob change. photo.thumbnailUrl (owned+revoked by the catalog store) is passed through untouched via `photo.thumbnailUrl ?? blobUrl`; only the locally-created blob URL is revoked. Removed the now-unused useMemo import.
- **`src/ui/components/TopBar.tsx`** — Removed the never-reset `armed` ref that gated the one-time reconnect gesture listener; the effect now re-arms the pointerdown listener whenever needsReconnect transitions false->true. Effect cleanup (keyed on needsReconnect) plus { once: true } already prevent duplicate listeners, so the ref only suppressed legitimate re-arming after a failed/re-revoked reconnect. Also dropped the now-unused useRef import.
- **`src/ui/components/Histogram.tsx`** — When data becomes null (photo closed/deselected), clear displayRef.current so the draw path takes the empty-state branch instead of rendering the previous photo's stale bins indefinitely. Confirmed: the data effect only populated displayRef when data was non-null, and the animation tick's else{draw()} path plus draw()'s `if (!d)` guard meant a non-null stale displayRef kept drawing the old histogram after data went null.
- **`src/modules/library/LibraryListRow.tsx`** — Blob URL minted by URL.createObjectURL(photo.thumbnailBlob) in the useMemo was never revoked, leaking one blob URL per row on unmount and on every blob change. Added a useEffect keyed on the component-owned URL (ownedUrl = null when the store-owned photo.thumbnailUrl exists) that calls URL.revokeObjectURL on cleanup, so only self-created blob URLs are revoked and the shared store URL is left alone. The useMemo/render path is unchanged.
- **`src/modules/library/use-culling-shortcuts.ts`** — Gated the hardcoded Backspace fallback for photo.remove on getBinding("photo.remove") === "Delete", replicating matchAction's altDef semantics so the alias is live only while the action keeps its default binding. Bare Backspace no longer removes photos after the user rebinds or unbinds photo.remove.
- **`src/modules/library/InfoPanel.tsx`** — Histogram effect no longer depends on the whole photo object. It now depends only on the fields the render consumes (photoId, thumbnailUrl, rotation, exif.colorTemperature, fileAccessNonce, editNonce) and reads the live photo inside the effect via useCatalogStore.getState(), so metadata mutations that replace the photo object (rating, flag, colour label, keyword edits) no longer retrigger a full headless develop-pipeline render. Deliberately kept rotation/thumbnailUrl/colorTemperature/fileAccessNonce as deps rather than the finding's literal [photoId, editNonce] because those are genuine histogram inputs (rotation feeds the loadPhotoImage cache key and baked thumbnail; thumbnailUrl swaps on background preview rebuild / cross-window edit; fileAccessNonce bumps on permission re-grant) and dropping them would regress correct re-render behavior.
- **`src/modules/library/RenamePhotoDialog.tsx`** — Extended filename validation regex from /[/\\]/ to /[/\\:*?\"<>|\x00-\x1f]/ (and updated the error message) so Windows/macOS/Linux-invalid characters and control chars are rejected in the dialog instead of silently passing and failing later at the on-disk rename. Followed the dialog's existing validate-and-block convention.
- **`src/dev/key-diag.ts`** — diagActive() now returns false when core.devtools' enable-state isn't yet meaningful: it requires sl_ext_default_seeded to include "core.devtools" (proving the default-off seeding recorded it) AND core.devtools to be absent from sl_ext_disabled. A fresh profile (or one predating seeding) no longer wrongly reads absence-from-disabled as "enabled", so the temporary key-diagnostic no longer monkey-patches window.addEventListener/removeEventListener or logs for normal users on first launch — matching the file's own 'installs ONLY when core.devtools is enabled' contract.
- **`src/update/update-checker.ts`** — checkForUpdateFull now selects the maximum-version channel-matching release via a reduce over compareSemver instead of taking GitHub's first (publish-date-ordered) result, so a later-published pre-release of an older line can no longer shadow a newer release and cause a false 'up-to-date'.
- **`src/raw/cache-bridge.ts`** — Added worker.onerror/onmessageerror handling in getWorker(): a fail() helper rejects the ready promise and every pending entry, clears the pending map, and resets worker/ready to null (guarded on worker===w) so the next cache call rebuilds a fresh worker instead of awaiting a never-settling ready promise forever. Also wrapped postMessage in send() in try/catch so a throwing send deletes its pending entry and rejects rather than leaking. Callers' existing try/catch in raw-cache.ts now correctly degrades to null/empty.
- **`src/raw/libraw.ts`** — getLibRaw no longer permanently memoizes module absence: when no module is present cached stays undefined (returning null via ?? null) so a later globalThis.__safelightLibRaw registration — the runtime flow the header documents — is picked up on the next call; a found module is still memoized via the truthy `if (cached) return cached` fast path, and registerLibRaw's reset/register behavior is preserved.
- **`src/raw/decode-pool.ts`** — warmDecodePool cached the first warming promise and ignored the size argument on later calls; added `|| size > poolSize` to the guard so a request to grow the pool re-runs the idempotent ensurePool(size).
- **`src/update/UpdateBanner.tsx`** — Confirmed: Close called onDismiss(false), which persisted nothing, so the 3-hour setInterval poll re-ran checkForUpdate (which only filters the persisted Skip via getDismissedVersion) and setUpdate(info) re-surfaced the same un-skipped version mid-session, contradicting the Close tooltip ('remind you again next launch'). Added a closedThisSession useRef<Set<string>>; the poll callback now returns early when info.version is in it, and dismiss(false) adds the version to it (skip still persists via dismissVersion). Suppression is session-only so the version reappears next launch, matching the tooltip; the module header comment refers to the persisted Skip path and stays correct.
