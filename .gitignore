# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Electron build output
release
build/icon.ico
build/icon.png

# Code-signing certificate (private key — never commit)
build/safelight-cert.pfx

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# AI assistant config, context and session artifacts — local working state, not
# part of the app. (Note: art/icon.ai is Illustrator, deliberately not matched.)
/.claude
/.claude-trace
/.codex
/.cursor
/.aider*
/.continue
/.gemini
/.specstory
/.windsurf
/.mcp.json
/.cursorrules
/.windsurfrules
/.aiderignore
AGENTS.md
CLAUDE.local.md
CLAUDE.md
GEMINI.md
.github/copilot-instructions.md
docs/superpowers/
task_plan.md
findings.md
progress.md

# Lensfun vendor database (5MB XML, regenerable — only ship the JSON)
src/lens-profiles/vendor/

# Local/test extensions — these live in their own repos and are not part of the
# app. Install them at runtime via the Extensions window like any other.
/extensions/
